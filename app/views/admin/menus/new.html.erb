<%= form_with model:@menu, url: admin_shop_menus_path do |f| %>
	<%= render 'layouts/error_messages', resource: @menu %>
	
	
<div class="flex">
	<div class="container">
  <div class="row no-gutters">
    <div class="col-md-5 col-sm-10 mx-2 mx-sm-auto shadow-lg rounded px-0 my-5">
      <h5 class="text-dark bg-light p-4 mb-0">メニュー新規登録</h5>
      <div class="form-body p-4">
<form>
<div><%= f.label :name, "メニュー名" %></div>
<div><%= f.text_field :name %></div>
				
<div><%= f.label :price, "価格" %></div>
<div><%= f.text_field :price %></div>

<div><%= f.label :genre_id, "ジャンル" %></div>
<div><%= f.collection_select :genre_id, Genre.all, :id, :name %></div>

<div class="flex align-center pl5">
	<%= f.submit "投稿", class: "btn btn-primary" %>
</div>

</form>
				</div>
		</div>
	</div>
</div>
</div>
    
<% end %>